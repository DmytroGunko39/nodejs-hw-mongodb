apis:
  sample@v1:
    root: docs/openapi.yaml

  theme:
    root: docs/openapi.yaml
    openapi:
      htmlTemplate: ./docs/index.html
      theme:
        colors:
          primary:
            main: '#32329f'
      generateCodeSamples:
        languages:
          - lang: curl
          - lang: Node.js
          - lang: JavaScript

extends:
  - recommended

rules:
  no-unused-components: error
